# Appendix Nginx - Senior Front-end Topics

Supplementary workbook for senior front-end engineers who need to understand Nginx in production:
request routing, static SPA hosting, reverse proxying, TLS, compression, caching, load balancing,
security controls, and debugging.

```bash
bun install
bun run --filter appendix-nginx dev
bun run --filter appendix-nginx build
```
